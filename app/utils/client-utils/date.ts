import type { Dayjs } from 'dayjs';

export function getCommitSince(): Dayjs | null {
  // At the moment we just set to null, so let user fetch as they like.
  // It's optional, anyway.
  return null;
}
